# Prefer PyPI release for security updates (CVEs in older git pin: deserialization, ReDoS, use-after-free).
# Use git ref only if a specific commit is required.
openai-whisper>=20240930
fastapi
uvicorn[standard]
python-multipart
aiofiles==24.1.0